# DESIGN.md

本项目是进程调度模拟器，不是课程介绍页。

设计目标是做出一个现代、克制、信息密度高的实验台：第一眼能看见当前算法、运行进程、就绪队列、终止队列和调度日志。

# 气质

参考 Vercel / Geist 的方向：黑白灰为主，网格清楚，信息紧凑，细节准确。

界面要像工具，不像宣传页。不要 hero，不要大段说明，不要装饰性插画。

# 布局

第一屏直接进入模拟器。

- 左侧：算法选择、配置、测试数据。
- 顶部：运行状态和主要操作。
- 中间：当前运行进程、就绪队列、终止队列。
- 下方：调度日志、时间线、统计图表。

所有内容都应有明确对齐关系。优先使用 grid / flex，不用 JS 测量布局。

桌面端保持密集网格。移动端允许纵向堆叠，但不能丢失核心状态。

# 色彩

主色调是黑、白、灰。

浅蓝等中性色只用于区分轻量状态。克莱因蓝用于最高强调，例如当前运行进程、主操作、关键选中态。

建议色值：

- Background：`#ffffff`
- Foreground：`#111111`
- Muted：`#f5f5f5`
- Border：`#e5e5e5`
- Subtle Text：`#6b7280`
- Soft Blue：`#eaf3ff`
- Klein Blue：`#002FA7`
- Error：`#dc2626`
- Success：`#16a34a`

颜色要有角色，不要随手选色。状态不能只靠颜色表达，要同时有文字、图标或位置变化。

# 字体

使用 `MiSans`。

数字、时间片、PID、优先数、剩余时间等对齐信息使用等宽或 tabular nums。对比型数据必须容易扫读。

标题保持短。界面文字不要写成说明文。

# 组件

优先使用 shadcn/ui Blocks，尤其是 dashboard 类型的 blocks。

基础组件沿用 shadcn/ui：Button、Card、Table、Tabs、Dialog、Sheet、Select、Input、Slider、Badge、Sidebar、Chart。

不要混用 Ant Design、MUI、Mantine 这类完整组件库。

卡片只承载明确的信息块，不做装饰容器。不要卡片套卡片。

# 状态表达

页面最重要的是让人看懂状态变化。

必须清楚展示：

- 当前算法
- 当前运行进程
- 就绪队列
- 终止队列
- PCB 使用情况
- 本次调度原因
- 调度日志

当前运行进程使用最高视觉层级。就绪队列和终止队列保持可扫读，不要用大面积强色。

# 动效

动效只解释状态变化。

使用 `motion` 的 `layout` 处理进程卡片在就绪队列、运行区、终止区之间的位置变化。

新增进程、进程终止、日志追加只做轻量淡入淡出。

不做粒子、发光背景、拖拽动画和持续循环动效。动画要响应用户操作，并尊重 `prefers-reduced-motion`。

# 文案

文案极简。

按钮和标题使用核心名词，例如：

- 单步
- 自动运行
- 暂停
- 重置
- 生成进程
- 就绪队列
- 运行中
- 终止队列

不要在主界面解释“什么是进程调度”。说明内容放到报告或帮助入口。

# 可访问性

所有可点击元素必须有可见 focus 状态。

图标按钮必须有 `aria-label` 或可见文字。

表单必须有 label。错误提示放在对应字段附近。

不要禁用浏览器缩放。移动端输入字号不低于 16px。

# 禁止项

- 不做 landing page。
- 不做渐变球、粒子、发光背景。
- 不用大段介绍文案填充页面。
- 不把颜色当作唯一状态提示。
- 不为了美观隐藏课程设计要求。
- 不在 UI 里混入多套组件体系。

# 参考

- Vercel Geist：高对比色彩、网格、开发者工具气质。
- Vercel Web Interface Guidelines：交互、布局、动效、表单和可访问性细节。
- Google Material Design：布局负责组织界面结构，颜色分为 surface / accent / semantic，组件是可复用的交互构件，动效只应帮助理解变化。
